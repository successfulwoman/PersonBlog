# 二次元萌系粉蓝风格 · Android 应用主题设计规范

> 本规范提取自 **astro-koharu** 博客主题（设计灵感源自 Hexo Shoka 主题）的完整视觉体系，
> 用于在 Android 应用中还原「二次元 / 萌系 / 粉蓝」风格。本文档为**纯设计规范**，
> 不含具体代码实现，可作为 UI 设计稿、设计评审与开发对照的共同依据。

---

## 1. 主题概述

### 1.1 设计理念

- **风格定位**：二次元（ACG）萌系风格，核心关键词 —— **可爱（kawaii）、温暖、轻盈、少女感**。
- **名字由来**：主题名 "Koharu" 取自日语「小春日和（こはるびより）」—— 晚秋初冬时节如春日般温暖晴朗的日子，暗示**温柔、治愈、明亮**的产品气质。
- **色彩基调**：粉蓝配色。以**樱花粉（#e91e63 / #E95469）为主色**，搭配奶油白、薄荷绿、淡粉紫等低饱和辅助色，深色模式则采用暖灰蓝夜色背景。
- **气质关键词**：圆润（Rounded）、渐变（Gradient）、柔光（Soft Glow）、治愈（Healing）、轻盈（Airy）。

### 1.2 应用原则（Android 侧）

| 原则 | 说明 |
| --- | --- |
| 萌而不幼稚 | 粉系配色只用于主色与强调色，正文、图表保持中性灰，避免整体过于甜腻 |
| 圆润统一 | 所有可交互元素统一圆角体系（见 §4），禁止直角/圆角混用 |
| 深浅双主题 | 必须同时提供浅色 / 深色两套完整色板（见 §2），并支持跟随系统 |
| 动效克制 | 装饰性动效（波浪、粒子）默认轻量运行，且必须响应系统"减少动态效果"设置 |
| 二次元元素点缀 | 插画、看板娘、贴纸作为**氛围装饰**，不遮挡核心信息 |

---

## 2. 色彩系统

### 2.1 品牌主色（Primary）

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| 樱花粉 Pink（Logo 主色） | `#E91E63` | 品牌 Logo、主按钮、选中态、链接 |
| Shoka 红粉（主题强调色） | `#E95469` | 强调、徽章、发光效果 |
| Shoka 亮粉 | `#FF6B7A` | 浅色背景上的强调色、高亮 |
| Shoka 暗粉 | `#D63F55` | 深色背景上的强调色、按压态 |

> 浅色模式主色建议取 `hsl(351, 77%, 62%)`（约 #E86780 一档的粉红），
> 深色模式主色建议取 `hsl(350, 77%, 70%)` 的亮粉，保证两种模式下与背景的对比度。

### 2.2 粉色阶（Mandy Scale）

完整粉色阶（可作主色的衍生色板，用于按钮 hover、标签、图表系列色）：

| 阶 | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 色值 | #FEF2F3 | #FDE6E8 | #FBD0D4 | #F7AAB2 | #F27A8A | #E91E63 | #D42A4C | #B21E3F | #961B3B | #801B38 | #470A1A |

### 2.3 浅色模式（Light）语义色板

| Token | 色值 | 用途 |
| --- | --- | --- |
| Background | `hsl(0, 12%, 99%)`（≈ #FDFCFC） | 页面背景 |
| Foreground | `hsl(0, 0%, 20%)`（≈ #333） | 正文、标题 |
| Card / Surface | 同 Background | 卡片、弹层表面 |
| Surface Variant | `hsl(240, 4.8%, 95.9%)` | 次要容器、输入框底、分区底色 |
| Muted Foreground | `hsl(0, 0%, 35%)` | 次要文字、占位符 |
| Border / Divider | `hsl(240, 5.9%, 90%)` | 描边、分割线、输入框边框 |
| Ring / 焦点环 | `hsl(346.8, 77.2%, 49.8%)` | 焦点高亮、选中描边 |
| Badge Primary | `hsl(351, 49%, 50%)` | 徽章、角标 |

### 2.4 深色模式（Dark）语义色板

| Token | 色值 | 用途 |
| --- | --- | --- |
| Background | `hsl(216, 13%, 15%)`（≈ #21252B，暖灰蓝夜） | 页面背景 |
| Foreground | `hsl(0, 0%, 95%)` | 正文、标题 |
| Card / Surface | 同 Background | 卡片、弹层表面 |
| Surface Variant | `hsl(216, 13%, 22%)` | 次要容器、输入框底 |
| Muted Foreground | `hsl(0, 0%, 63%)` | 次要文字 |
| Border / Divider | `hsl(216, 13%, 25%)` | 描边、分割线 |
| Ring / 焦点环 | `hsl(346.8, 77.2%, 49.8%)`（同浅色） | 焦点高亮 |

> 深色背景刻意带一点**蓝灰暖调**（而非纯黑），是"二次元深夜模式"的灵魂 —— 在 OLED 屏上建议用 `#21252B → #000` 的垂直渐变过渡，避免纯黑过于生硬。

### 2.5 渐变系统（Gradient）

渐变是本主题最具识别度的视觉语言，Android 端建议用 Brush / drawable 原样还原：

| 渐变 | 定义 | 用途 |
| --- | --- | --- |
| 主题粉 Gradient Pink | `279° #F96676 → #E91E63` | 主按钮、强调卡片、渐变图标 |
| 页面背景 Background | `180° #FDFBFB → #EBEDEE`（浅）/ `#21252B → #000`（深） | 页面整体背景 |
| 页头渐变 Header | `-225° #E3FDF5 → #FFE6FA`（薄荷绿→粉，浅）/ `#2D3230 → #322D31`（深） | 顶部区域、大封面背景 |
| 按钮渐变 Shoka Button | `→ #ED6EA0 → #EC8C69`（粉→蜜桃，浅）/ `#F18BB3CC → #F0A387CC`（深） | 主 CTA、特别强调按钮 |
| 网格底色 Grid | 浅：`#ED719A #FFACDE #FBD7ED #EEF1F0 #DDDDDD #B4B4B4`；深：`#212832 #3F4659 #8592A7 #EEEFEA` | 空态背景、占位图案、马赛克装饰 |

> 页头渐变（薄荷绿 + 樱花粉）是「粉蓝风格」的直接体现，适合用于 App 的启动页、Banner、空状态插画底色。

### 2.6 语义色与文本强调色

**状态色（随深浅模式微调亮度）**：

| 状态 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| Info 信息 | `hsl(210, 100%, 50%)` | `hsl(210, 100%, 65%)` | 提示条、链接类信息 |
| Success 成功 | `hsl(142, 76%, 36%)` | `hsl(142, 76%, 50%)` | 成功反馈 |
| Warning 警告 | `hsl(45, 100%, 40%)` | `hsl(45, 100%, 55%)` | 警告提示 |
| Danger 危险 | `hsl(0, 84%, 60%)` | `hsl(0, 84%, 70%)` | 删除、错误 |

**文本强调色（浅色 / 深色两套）**：用于 Markdown 内容、标签文字、重点段落。

| 名称 | 浅色 | 深色 | 名称 | 浅色 | 深色 |
| --- | --- | --- | --- | --- | --- |
| 红 Red | #E74C3C | #F87171 | 青 Aqua | #1ABC9C | #2DD4BF |
| 粉 Pink | #E91E63 | #F472B6 | 蓝 Blue | #2E86C1 | #60A5FA |
| 橙 Orange | #F39C12 | #FBBF24 | 紫 Purple | #8E44AD | #C084FC |
| 黄 Yellow | #D4A017 | #FACC15 | 灰 Grey | #7F8C8D | #9CA3AF |
| 绿 Green | #27AE60 | #4ADE80 | | | |

### 2.7 节日变体（可选：圣诞主题）

主题支持节日换肤机制（圣诞特效），可作为 Android 节日运营活动的参考：

- 圣诞红 `hsl(0, 70%, 45%)`（浅）/ `hsl(0, 65%, 55%)`（深）、圣诞绿 `hsl(150, 60%, 35%)`（浅）/ `hsl(150, 55%, 50%)`（深）、圣诞金 `hsl(43, 75%, 55%)`（浅）/ `hsl(43, 70%, 60%)`（深）
- 背景渐变 `#FFF9F9 → #F0F8F0`（浅）/ `#1A1512 → #0D1A0D`（深）
- 实现方式建议：**以「覆盖变量」的方式换肤**（类似 CSS 变量覆盖），而非新建整套主题，保证换肤成本可控。

### 2.8 颜色使用规范

- **60-30-10 法则**：60% 中性背景（Background/Surface）＋ 30% 表面与内容（Card/Foreground）＋ 10% 粉色强调。
- 主色（粉）仅用于**一个层级**的强调（如主按钮、选中态、链接），不要大面积铺陈。
- 正文对比度必须满足 **WCAG AA（正文 ≥ 4.5:1，大字号 ≥ 3:1）**。
- 禁止在纯白/纯黑背景上直接使用浅粉、浅灰等低对比色（浅色背景统一用 `#FDFCFC` 而非 #FFF）。
- 渐变使用时保证两端色相相近（同色系渐变），避免红绿撞色。

---

## 3. 字体排印

### 3.1 字体选择

| 层级 | 字体 | 说明 |
| --- | --- | --- |
| 品牌字体 | **寒蝉全圆体（Chill Round）** | 圆润无衬线圆体，是"萌系感"的第一来源，用于标题、品牌字、按钮 |
| 系统回退 | Roboto / 系统默认无衬线 | 长文正文、数据密集场景 |

> **设计要点**：圆体字形圆润、笔画端头为圆形，天然传达可爱感。Android 端下载并嵌入该字体（或选择风格相近的圆体开源字体）时，仅用于**标题与品牌场景**，正文优先系统字体以保证渲染性能与可读性。

### 3.2 字号阶梯（px 数值可直接按 1:1 映射为 dp）

| 名称 | 字号 | 行高 | 字距 | 用途 |
| --- | --- | --- | --- | --- |
| xs | 12 | 16 | +0.025em | 辅助说明、角标 |
| sm | 14 | 20 | +0.0125em | 次要文字、注释 |
| base | 16 | 24 | 0 | 正文（默认） |
| lg | 18 | 28 | -0.0125em | 强调正文、列表标题 |
| xl | 20 | 28 | -0.025em | 小标题 |
| 2xl | 24 | 32 | -0.025em | 区块标题 |
| 3xl | 30 | 36 | -0.025em | 页面标题 |
| 4xl | 36 | 40 | -0.025em | 大标题 |
| 5xl / 6xl / 7xl | 48 / 60 / 72 | 1（即字号） | -0.025em | 品牌页、启动页大字 |
| 8xl / 9xl | 96 / 128 | 1 | -0.025em | 海报级装饰字 |

**规律**：字号越大，字距越紧（负字距）；字号越小，字距越松。Android 中建议用 sp 并允许系统字体缩放。

### 3.3 字重

thin(100) → black(900) 全梯度可用。规范建议：

- 标题 / 品牌：**Bold(700) ~ Black(900)**
- 正文：**Normal(400)**
- 强调词 / 按钮：**Medium(500) ~ SemiBold(600)**

---

## 4. 形状与圆角

### 4.1 圆角体系

| Token | 数值 | 建议用途（Android） |
| --- | --- | --- |
| sm | 2dp | 小标签、内嵌元素 |
| md | 6dp | 输入框、次级按钮 |
| lg（默认） | **12dp** | **卡片、对话框、主按钮、列表项**（默认半径 `--radius: 12px`） |
| xl | 16dp | 大卡片、Bottom Sheet |
| 2xl | 24dp | 插画容器、头像卡、空态 |
| 3xl | 24dp+ | 大图卡、横幅 |
| full | 9999dp | **头像、图标按钮、胶囊按钮、标签**（圆形/胶囊） |

### 4.2 造型规则

- **卡片默认 12dp 圆角**，是整套 UI 的"圆润感基线"。
- 头像、图标容器一律 **full（圆形）**。
- 按钮可二选一：胶囊形（full）或 12dp，但**全 App 必须统一**。
- 图片装饰可做**斜切角**处理（对角线裁切，如 `polygon(0 0, 92% 0, 100% 100%, 0 100%)`），是 Shoka 系的标志性造型，适合封面图、头图。
- 与 Material 3 大圆角语言天然兼容，直接映射 `Shapes` 的 small / medium / large 即可。

---

## 5. 阴影与层级

### 5.1 阴影体系（Elevation 0–4）

| Token | 定义 | 用途 |
| --- | --- | --- |
| sm | `0 1px 2px rgba(0,0,0,.05)` | 极浅层级（分割感） |
| DEFAULT | `0 1px 3px + 0 1px 2px -1px (α.1)` | 默认卡片 |
| md | `0 4px 6px -1px + 0 2px 4px -2px (α.1)` | 悬浮卡片、列表抬升 |
| lg | `0 10px 15px -3px + 0 4px 6px -4px (α.1)` | 下拉、弹层 |
| xl | `0 20px 25px -5px + 0 8px 10px -6px (α.1)` | 对话框 |
| 2xl | `0 25px 50px -12px (α.25)` | 全屏浮层 |

**专用阴影**：

- **卡片阴影** `0 10px 30px rgba(90,97,105,.12)`（hover 加深 .2）—— 全 App 卡片默认；
- **Shoka 按钮光晕** `0 0 16px rgba(233,84,105,.8)` —— 主按钮/强调元素周围的**粉色柔光**，二次元感最强的细节。

### 5.2 层级规则

- 阴影要"轻"：全部使用低透明度黑，α ≤ 0.25，避免生硬。
- 深色模式下阴影可加深至 α.5 左右（深色背景上阴影需更明显）。
- 粉色光晕只给**主 CTA 和选中态**使用，一次页面不超过 1 处。

---

## 6. 间距与布局

### 6.1 间距体系（4dp 基准）

| Token | 值 | Token | 值 |
| --- | --- | --- | --- |
| xs | 8 | lg | 24 |
| sm | 12 | xl | 32 |
| md | 16 | 2xl / 3xl / 4xl | 48 / 64 / 96 |

### 6.2 布局规则

- 页面两侧安全边距 **16dp**，卡片内边距 **16~24dp**，卡片间距 **16dp**。
- 移动端优先：单列卡片流；内容最大宽度约 **1400dp（桌面/平板）**，卡片自适应。
- 底部区域可留出装饰空间（波浪、贴纸），但需避开系统手势区与导航栏。
- 触控目标 **≥ 48dp**（图标按钮 40dp 时需扩大热区）。

---

## 7. 动效设计

### 7.1 时长体系

| Token | 时长 | 用途 |
| --- | --- | --- |
| fast | 150ms | 按压反馈、颜色变化 |
| tween / normal | 200 / 250ms | 常规切换、淡入淡出 |
| ui | 300ms | 通用交互动效 |
| slow / slower | 350 / 500ms | 页面转场、面板展开 |
| flipCard | 600ms | 卡片翻转 |

### 7.2 缓动曲线

| 名称 | 曲线 | 用途 |
| --- | --- | --- |
| easeOut | `cubic-bezier(0, 0, 0.2, 1)` | 元素进入（默认） |
| easeInOut | `cubic-bezier(0.4, 0, 0.2, 1)` | 常规过渡 |
| **spring** | `cubic-bezier(0.34, 1.56, 0.64, 1)` | **弹性回弹（萌感核心）**，适合点赞、选中、弹出 |
| easeIn | `cubic-bezier(0.4, 0, 1, 1)` | 元素退出 |

> 弹簧参数参考：stiffness 150–500、damping 9–35（如"wobbly"刚度 400/阻尼 20、"gentle"刚度 200/阻尼 25）。**弹性动画是二次元 UI 的辨识度来源**，但要克制：只用于点赞、收藏、弹出、选中这类小交互。

### 7.3 标志性动效

| 动效 | 规格 | 应用建议 |
| --- | --- | --- |
| **波浪动画** | SVG 波形沿 X 轴平移循环（12s / 7s / 4s 三层错开，线性） | 启动页底部、页面底部装饰、播放页背景 |
| 上下摇晃 | 缩放 + 旋转 ±3°（约 1s） | 卡片 hover、抽卡/福袋类趣味交互 |
| 卡片翻转 | 3D 翻转（透视 1000px，600ms） | 卡片正反面信息 |
| 滑入滑出 | 12dp 位移 + 淡入（200ms） | 列表增删、菜单 |
| 手风琴展开 | 高度 0 → 自适应（200ms） | 折叠面板 |
| 渐隐占位 | 图片加载前渐变色占位（LQIP） | 列表图片加载 |
| 转圈 / 光标 | 常速旋转 / 打字机光标 | 加载、引导 |

### 7.4 动效规范

- 所有装饰性动效必须响应系统的「减少动态效果」设置（Android：`Settings.Global.ANIMATOR_DURATION_SCALE` 或 `AccessibilityManager`），关闭时退化为静态或仅保留必要反馈。
- 波浪等循环动效默认**低频运行**，且在非前台/无内容区域自动暂停。
- 动效时长遵循 §7.1，不得随意延长或叠加。

---

## 8. 二次元元素应用指南

### 8.1 插画与看板娘（Mascot）

- **看板娘设定**：建议为 App 设计 1 位固定看板娘（参考博客的 LL&CC 矢量 Logo 角色化），用于：启动页、空状态、错误页、加载页、设置页头部。
- 插画风格：**平涂 + 柔光渐变**（与 Gradient Pink 同色系）、粗圆润描边或无线描、大眼萌系比例（Q 版 2~3 头身）。
- 表情包化：为看板娘提供 4~6 个表情（默认 / 开心 / 加载中 / 空 / 错误 / 晚安），对应不同 UI 状态。
- 尺寸规范：空状态插画建议 160~200dp；启动页角色 240~320dp；头像 48~96dp 圆形裁切。
- **禁止**：插画遮挡文字内容、插画参与交互反馈的加载逻辑（避免误导）。

### 8.2 头像与 Logo

- 头像一律**圆形**（full 圆角）＋ 1dp 粉色描边或柔光。
- Logo 主色为樱花粉 `#E91E63`，深色模式下可换亮粉 `#F472B6`。
- 品牌标识可加粉色光晕（§5.1 shoka-button 阴影）提升二次元"发光感"。

### 8.3 装饰元素（Decorations）

| 元素 | 建议 |
| --- | --- |
| 波浪（Wave） | 页底/封面底部多层 SVG 波浪，颜色取背景渐变起始色，透明度 0.6~0.75 叠加 |
| 渐变底纹 | 空态、Banner 使用页头渐变（薄荷绿→粉）或网格底色（马赛克粉蓝灰） |
| 贴纸 / 角标 | 粉色系圆形贴纸（如"NEW""已读完"），胶囊形，圆角 full |
| 星星 / 光斑 | 浅色背景点缀低透明度星星、气泡，仅限空态与启动页 |
| 斜切封面 | 图片卡片做对角线斜切角，强化插画感 |

### 8.4 文案与语气

- 采用**软萌口吻**：可使用颜文字、小表情符号点缀（如「加载中…… (｡•̀ᴗ-)✧」），但功能文案保持简洁明确。
- 错误文案不要生硬：建议"安抚 + 引导"式（如"网络开小差了，再试一次吧"），配合看板娘表情。
- 节日运营（圣诞等）可通过换肤变量整体切换主题色与装饰（§2.7），并配合应景文案。

### 8.5 素材清单建议

看板娘立绘（含透明背景 PNG/WebP）· 表情包 4~6 枚 · 圆形头像 · Logo（含单色/彩色/发光版）· 波浪 SVG × 3 层 · 空状态插画 × 3（空/错误/加载）· 贴纸包 · 启动页背景渐变。

---

## 9. 深色模式与主题切换

### 9.1 双主题要求

- **必须**完整提供浅色 / 深色两套色板（§2.3 / §2.4），并随系统自动切换，同时提供 App 内手动切换。
- 深色模式不是简单反色：背景用暖灰蓝（≈ #21252B），强调色提亮一档（主色 `hsl(350,77%,70%)`），文本强调色整体提亮（§2.6 深色列）。
- 渐变与阴影均需提供深色版本（§2.5 / §5）。

### 9.2 与 Material You 动态取色的关系

- **建议关闭（或默认关闭）动态取色（Monet）**，以保持品牌粉色的一致性 —— 本主题的粉色是品牌资产，不应被系统壁纸色替代。
- 若启用动态取色，需校验生成色与品牌色的色相接近度，并在设置中提供「跟随主题」/「经典粉」开关。

### 9.3 切换体验

- 主题切换应平滑过渡（建议 250~300ms），状态栏 / 导航栏颜色同步切换（浅色模式用浅色栏 + 深色图标，深色模式反之）。
- 壁纸 / 背景渐变随主题联动，避免出现"浅色主题 + 深色图片"的割裂。

---

## 10. 无障碍与可用性

| 项目 | 要求 |
| --- | --- |
| 文字对比度 | 正文 ≥ 4.5:1，大字 ≥ 3:1（WCAG AA），粉色主色上的文字必须使用深色或白色测试通过 |
| 触控目标 | ≥ 48dp（44dp 下限） |
| 字号缩放 | 支持系统字体缩放（sp），缩放至 200% 不破版 |
| 减少动效 | 遵循系统「减少动态效果」，关闭波浪/粒子/弹性动画 |
| 焦点指示 | 使用 Ring 色（`hsl(346.8,77.2%,49.8%)`）提供清晰焦点环 |
| 语义色不可单独依赖 | 成功/警告/危险除颜色外需配图标或文字 |

---

## 11. 设计 Token 总表（速查）

| 类别 | 核心值 |
| --- | --- |
| 主色 | #E91E63（Logo）/ #E95469（强调）/ hsl(351,77%,62%)（浅）/ hsl(350,77%,70%)（深） |
| 背景 | 浅 hsl(0,12%,99%)；深 hsl(216,13%,15%) |
| 前景 | 浅 hsl(0,0%,20%)；深 hsl(0,0%,95%) |
| 渐变 | 粉 279° #F96676→#E91E63；页头 -225° #E3FDF5→#FFE6FA；按钮 #ED6EA0→#EC8C69 |
| 圆角 | 2 / 6 / 12 / 16 / 24 / full（默认 12dp） |
| 阴影 | 4 级 elevation（α≤.25）；卡片 0 10px 30px rgba(90,97,105,.12)；粉光晕 0 0 16px rgba(233,84,105,.8) |
| 间距 | 4dp 基准：8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 |
| 字号 | 12 / 14 / 16 / 18 / 20 / 24 / 30 / 36 / 48 / 60 / 72 / 96 / 128 |
| 字体 | 寒蝉全圆体（品牌）/ 系统无衬线（正文） |
| 时长 | 150 / 200 / 250 / 300 / 350 / 500 / 600ms |
| 缓动 | easeOut `(0,0,.2,1)`、spring `(.34,1.56,.64,1)`、easeInOut `(.4,0,.2,1)` |
| 状态色 | Info hsl(210,100%,50%) / Success hsl(142,76%,36%) / Warning hsl(45,100%,40%) / Danger hsl(0,84%,60%)（深色各提亮） |

---

## 12. 规范来源与追溯

本规范所有数值均提取自 astro-koharu 项目源码（Tailwind 配置、设计 Token 与主题 CSS），如需核对原始定义可查阅：

- `src/constants/design-tokens.ts` —— 色彩 / 间距 / 字号 / 阴影 / 圆角 / 动效 Token
- `src/styles/theme/shadcn.css` —— 语义色板（浅 / 深）
- `src/styles/theme/index.css` —— 渐变、Shoka 色、文本强调色
- `src/styles/christmas/christmas-theme.css` —— 节日换肤示例
- `src/styles/components/wave.css` —— 波浪动效
- `tailwind.config.mjs` —— 字体（寒蝉全圆体）、阴影、动画汇总
