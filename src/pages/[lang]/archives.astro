---
import { getLocaleStaticPaths } from '../_shared/utils';
import ArchivesPage from '../archives.astro';
export const getStaticPaths = getLocaleStaticPaths;

interface Props {
  locale: string;
}

const { locale } = Astro.props;
---
<ArchivesPage locale={locale} />
