---
# ============================================================
# 发帖模板（复制本文件后改名使用）
# 用法：把本文件复制到分类目录，改名为英文/拼音（如 my-post.md），
# 然后修改下面的 frontmatter 和正文即可。
# 注意：本文件以 _ 开头，不会出现在网站上，放心留着当模板。
# ============================================================

title: 你的文章标题          # 必填：文章标题
date: 2026-02-01 20:00:00   # 必填：发布日期（格式：年-月-日 时:分:秒）
description: 文章简介，用于列表和搜索引擎（不填则自动从正文提取）
link: my-post               # 可选：自定义网址（不填则用文件名）
cover: /img/custom/cover/1.webp  # 可选：封面图（放在 public/img/ 下）
tags:                       # 可选：标签
  - 随笔
categories:                 # 可选：分类（分类名必须存在于 config/site.yaml 的 categoryMap）
  - 随笔
sticky: false               # 可选：true = 置顶
draft: false                # 可选：true = 草稿（正式发布时不显示）
catalog: true               # 可选：是否显示侧边目录（默认 true）
---

# 你的文章标题

这里是正文，支持 Markdown 语法：

**加粗**、*斜体*、`行内代码`

> 引用内容

- 列表项一
- 列表项二

| 列一 | 列二 |
| --- | --- |
| 内容 A | 内容 B |

![图片说明](/img/custom/cover/1.webp)

```js
console.log('Hello, World!');
```
